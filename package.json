{
  "name": "mywebbun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build:stoov-test": "cd stoov-test && bun run build",
    "build": "bun run build:stoov-test",
    "start": "bun run --smol src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "hono": "^4.13.3"
  }
}
