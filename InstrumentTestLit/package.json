{
  "name": "instrument-test-lit",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build && bun server.ts",
    "build": "bun build index.html --outdir dist --target browser --format esm --minify",
    "watch": "bun build index.html --outdir dist --target browser --format esm --watch",
    "typecheck": "tsc --noEmit",
    "start": "bun server.ts"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "@lit/task": "^1.0.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}